/// Stale Flutter-counter widget test removed.
/// Pumping [MyApp] needs dotenv + Firebase — covered later as a real widget test.
///
/// See test/README_TESTING.md for the unit-test learning suite.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('test suite smoke — 1 + 1', () {
    expect(1 + 1, 2);
  });
}
